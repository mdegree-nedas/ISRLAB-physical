version: '3'
services:
    redis:
        image: redis
        ports:
            - "6379:6379"

    publisher:
        build: ./init/publisher
        volumes:
            - ./src/publisher:/root/workspace
        depends_on:
            - redis
